export SERVER_PORT=8080
export SPRING_FLYWAY_ENABLED=true

# identity (MUST match OriginTier enum: leaf|subzone|zone|central)
export SYNC_TIER=central
export SYNC_ZONE=_
export SYNC_SUBZONE=_
export SYNC_NODE_ID=central-1

# connect to local Central NATS
export NATS_URL="nats://127.0.0.1:4222"

# enable components
export SYNC_BOOTSTRAP_ENABLED=true
export SYNC_RELAY_ENABLED=false
export SYNC_OUTBOX_ENABLED=true
export SYNC_OUTBOX_MAX_RETRIES=5
export SYNC_POC_ADMIN_ENABLED=true

# ---- Streams owned on Central ----
export SYNC_UP_ZONE_STREAM_ENABLED=true
export SYNC_DOWN_CENTRAL_STREAM_ENABLED=true

# Keep Central UP sink broad so Leaf-C direct publishes land here too
export SYNC_UP_ZONE_STREAM_SUBJECT="up.*.>"
export SYNC_DOWN_CENTRAL_STREAM_SUBJECT="down.central.>"

# Disable other streams on Central for clarity
export SYNC_UP_LEAF_STREAM_ENABLED=false
export SYNC_UP_SUBZONE_STREAM_ENABLED=false
export SYNC_DOWN_ZONE_STREAM_ENABLED=false
export SYNC_DOWN_SUBZONE_STREAM_ENABLED=false
